# 6. Объединить массивы А размером 7 и В размером 8, предвари-
# тельно удалив максимальные элементы этих массивов. Результат по-
# лучить в массиве А. Удаление элемента массива с заданным индексом
# осуществить в методе.

# МАССИВЫ А И Б
A = [12, 100, 1234, 1, 312, 665, 4]
B = [0, 1, 3, 45 ,6, 234, 77, 2]
print('ИСХОДНЫЕ МАССИВЫ:', A, B, sep='\n')


# функция удаления элемента из массива по индексу
def func(massiv, index_max):
    new_massiv = []
    for i in range(len(massiv)):
        if i != index_max:
            new_massiv.append(massiv[i])
    return new_massiv
#проверка функции
A = func(A, A.index(max(A)))
B = func(B, B.index(max(B)))
for i in B:
    A.append(i)
print("РЕЗУЛЬТАТ:", A, sep='\n')
